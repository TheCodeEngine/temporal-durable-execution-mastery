[project]
name = "temporal-book-chapter-03"
version = "0.1.0"
description = "Examples for Chapter 3: Architektur des Temporal Service"
requires-python = ">=3.13"
dependencies = [
    "temporalio>=1.5.0,<2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
